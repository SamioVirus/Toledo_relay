# UI studio: implement the responsive slice

Implement only the approved UI handoff in the isolated worktree. Read the project instructions and existing components/styles first. Reuse tokens and patterns; keep changes accessible, responsive, and minimal. Cover loading, empty, error, focus, keyboard/touch, and narrow-width behavior when they are in scope.

Run the relevant tests and, when available, inspect the actual rendered surface at desktop and mobile widths. Capture exact evidence and note unavailable browser/screenshot proof instead of claiming it. Do not expose secrets or raw private ledger text. Do not change unrelated product behavior. Return changed paths, verification, render evidence, limitations, and evidence maturity. Choose `continue` when ready for critique.
